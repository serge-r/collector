Value Required NAME (\S+)
Value MEM (\d+)
Value VCPU (\d+)
Value List DISKNAMES ((?P<diskindex>\d+)\.name=(?P<name>\w+))
Value List DISKPATHS ((?P<diskindex>\d+)\.path=(?P<path>\S+))
Value List,Required DISKSIZES ((?P<diskindex>\d+)\.capacity=(?P<size>\d+))

# Named group in regex will return dict like that
# {'index': '1', 'name': 'hda'}

Start
  ^Domain: '${NAME}'
  ^(\s+)balloon\.maximum=${MEM}
  ^(\s+)vcpu\.maximum=${VCPU}
  ^(\s+)block\.${DISKNAMES}
  ^(\s+)block\.${DISKPATHS}
  ^(\s+)block\.${DISKSIZES}
  ^$$ -> Record




